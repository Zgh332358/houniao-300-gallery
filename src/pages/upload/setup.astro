---
/**
 * AI 助手设置已移至 /login 的第 3 步。
 * 访问此页面会重定向到 /login。
 */
const base = import.meta.env.BASE_URL.replace(/\/+$/, '');
---

<meta http-equiv="refresh" content={`0;url=${base}/login/`} />
<script define:vars={{ base }}>
	location.replace(base + '/login/');
</script>
